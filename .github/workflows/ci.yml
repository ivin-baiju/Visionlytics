name: Visionlytics CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      # The tracking test only needs the lightweight numerical/CV stack.
      # Avoid installing the full YOLO + Streamlit runtime in CI.
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy scipy opencv-python-headless

      - name: Run tracking tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: python tests/test_tracking.py
