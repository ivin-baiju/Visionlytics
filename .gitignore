# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environment
.venv/
venv/
ENV/
env.bak/
venv.bak/

# Environment variables
.env
.env.local

# IDEs
.idea/
.vscode/
*.swp

# Streamlit
.streamlit/credentials.toml

# macOS
.DS_Store

# Data / Models
# YOLOv8 weights are large (~22MB) — keep them tracked so the app works out of the box.
# But ignore training artifacts and temporary outputs.
outputs/
*.log

# Trained ML model artifacts (regenerated via train.py)
models/*.joblib

# Temporary files
*.tmp
*.temp
