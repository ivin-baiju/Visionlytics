version: '3.8'

services:
  visionlytics:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    # For live camera access in docker, uncomment the following line (Linux only)
    # devices:
    #   - "/dev/video0:/dev/video0"
